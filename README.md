# Solidity fundraiser challenge

The code is in `contracts/challenge.sol`.
The test code is in `test/sample-test.js`.


Try running some of the following tasks:

```shell
npx hardhat test
npx hardhat coverage
```
